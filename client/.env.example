# Origin of the weather API. Leave empty to call the same origin as the SPA.
VITE_API_BASE_URL=

# Target used by the Vite dev server proxy for /api requests
VITE_DEV_API_TARGET=http://localhost:3001
