# HTTP port for the API (App Service sets this automatically)
PORT=3001

# Comma separated list of browser origins allowed to call /api.
# Leave empty for same-origin hosting.
ALLOWED_ORIGINS=

# Requests allowed per IP per minute on /api
RATE_LIMIT_MAX=120

# Directory containing the built SPA (defaults to dist/public)
# CLIENT_DIST_DIR=
